import { CategorySortPipe } from './category-sort.pipe';

describe('CategorySortPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorySortPipe();
    expect(pipe).toBeTruthy();
  });
});
